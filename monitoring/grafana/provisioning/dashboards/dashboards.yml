apiVersion: 1

providers:
  - name: 'API Dashboard'
    orgId: 1
    folder: 'FastAPI'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
